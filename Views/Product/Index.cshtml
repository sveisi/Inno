@model IEnumerable<ProductListView>

@{
    ViewData["Title"] = Res.List + " " + Res.Product;

    ViewBag.CreateUrl = Url.Action("Create", "Product");
    ViewBag.EditUrl = Url.Action("Edit", "Product");
    ViewBag.DeleteUrl = Url.Action("Delete", "Product");
}
<form asp-action="Index" method="get">
    <div class="row mb-3 border-bottom">
        <div class="col-auto">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="col-auto">
            <a href="javascript:createItem()" class="btn btn-primary btn-sm mt-1 btnCreate">
                <i class="fa fa-plus me-1"></i>@Res.New
                <span class="spinner-border spinner-border-sm" style="display:none"></span>
            </a>
        </div>
    </div>
</form>
<form>
    @Html.AntiForgeryToken()
    <table id="myTable" class="table table-striped table-hover nowrap" style="width:100%">
        <thead class="bg-warning"></thead>
    </table>
</form>

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            var table = $('#myTable').DataTable({
                info: false,
                ajax: {
                    url: '@Url.Action("GetData", "Product")',
                    type: 'POST',
                    datatype: "json",
                    error: function (jqXHR, textStatus, errorThrown) {
                        if (jqXHR.status == 403 || jqXHR.status == 401) {
                            window.location = '@Url.Action("Login", "Account")';
                        }
                    }
                },
                "columns": [
                    { data: "@nameof(ProductListView.Id)", name: "@nameof(ProductListView.Id)", title: "@Html.DisplayNameFor(model => model.Id)" },
                    { data: "@nameof(ProductListView.Name)", name: "@nameof(ProductListView.Name)", title: "@Html.DisplayNameFor(model => model.Name)" },
                    { data: "@nameof(ProductListView.EnName)", name: "@nameof(ProductListView.EnName)", title: "@Html.DisplayNameFor(model => model.EnName)" },
                    { data: "@nameof(ProductListView.CategoryName)", name: "@nameof(ProductListView.CategoryName)", title: "@Html.DisplayNameFor(model => model.CategoryName)" },
                    { data: "@nameof(ProductListView.UnitName)", name: "@nameof(ProductListView.UnitName)", title: "@Html.DisplayNameFor(model => model.UnitName)" },
                    { data: "@nameof(ProductListView.ColorName)", name: "@nameof(ProductListView.ColorName)", title: "@Html.DisplayNameFor(model => model.ColorName)" },
                    { data: "@nameof(ProductListView.Price)", name: "@nameof(ProductListView.Price)", title: "@Html.DisplayNameFor(model => model.Price)" },
                    renderActionButton('@nameof(ProductListView.Id)', [editButtonConfig, deleteButtonConfig])
                ]
            });
        });
    </script>
}