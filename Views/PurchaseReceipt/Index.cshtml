@model IEnumerable<DocumentListView>

@{
    ViewData["Title"] = Res.List + " " + Res.Rolls;

    ViewBag.CreateUrl = Url.Action("Create", nameof(Document));
    ViewBag.EditUrl = Url.Action("Edit", nameof(Document));
    ViewBag.DeleteUrl = Url.Action("Delete", nameof(Document));
}

<form asp-action="Index" method="get">
    <div class="row mb-3 border-bottom">
        <div class="col-auto">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="col-auto">
            <a href="javascript:createItem()" class="btn btn-primary btn-sm mt-1 btnCreate">
                <i class="fa fa-plus me-1"></i>@Res.New
                <span class="spinner-border spinner-border-sm" style="display:none"></span>
            </a>
        </div>
    </div>
</form>
<form>
    @Html.AntiForgeryToken()
    <table id="myTable" class="table table-striped table-hover nowrap" style="width:100%">
        <thead class="bg-warning"></thead>
    </table>
</form>

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            var table = $('#myTable').DataTable({
                info: false,
                ajax: {
                    url: '@Url.Action("GetData", nameof(Document))',
                    type: 'POST',
                    datatype: "json",
                    error: function (jqXHR, textStatus, errorThrown) {
                        if (jqXHR.status == 403 || jqXHR.status == 401) {
                            window.location = '@Url.Action("Login", "Account")';
                        }
                    }
                },
                "columns": [
                    { data: "@nameof(DocumentListView.Id)", name: "@nameof(DocumentListView.Id)", title: "@Html.DisplayNameFor(model => model.Id)" },
                    { data: "@nameof(DocumentListView.ReferenceId)", name: "@nameof(DocumentListView.ReferenceId)", title: "@Html.DisplayNameFor(model => model.ReferenceId)" },
                    { data: "@nameof(DocumentListView.DocumentTypeId)", name: "@nameof(DocumentListView.DocumentTypeId)", title: "@Html.DisplayNameFor(model => model.DocumentTypeId)" },
                    { data: "@nameof(DocumentListView.StorageId)", name: "@nameof(DocumentListView.StorageId)", title: "@Html.DisplayNameFor(model => model.StorageId)" },
                    { data: "@nameof(DocumentListView.ObverseId)", name: "@nameof(DocumentListView.ObverseId)", title: "@Html.DisplayNameFor(model => model.ObverseId)" },
                    {
                        data: null, orderable: false, searchable: false, responsivePriority: -1,
                        render: function (data, type, row) {
                            return "<a href='javascript:editItem(" + data.@nameof(DocumentListView.Id) + ")' title='@Res.Edit'><i class='fa fa-edit'></i></a>";
                        }
                    },
                    {
                        data: null, orderable: false, searchable: false, responsivePriority: -1,
                        render: function (data, type, row) {
                            return "<a href='javascript:deleteItem(" + data.@nameof(DocumentListView.Id) + ")' class='link-danger' title='@Res.Delete'><i class='fa fa-times'></i></a>";
                        }
                    }
                ]
            });
        });
    </script>
}