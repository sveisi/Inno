@model IEnumerable<RegionView>

@{
    ViewData["Title"] = Res.List + " " + Res.Regions;

    ViewBag.CreateUrl = Url.Action("Create", "Region");
    ViewBag.EditUrl = Url.Action("Edit", "Region");
    ViewBag.DeleteUrl = Url.Action("Delete", "Region");
}

<form asp-action="Index" method="get">
    <div class="row mb-3 border-bottom">
        <div class="col-auto">
            <h3>@ViewData["Title"]</h3>
        </div>
        <div class="col-auto">
            <a href="javascript:createItem()" class="btn btn-primary btn-sm mt-1 btnCreate">
                <i class="fa fa-plus me-1"></i>@Res.New
                <span class="spinner-border spinner-border-sm" style="display:none"></span>
            </a>
        </div>
    </div>
</form>
<form>
    @Html.AntiForgeryToken()
    <table id="myTable" class="table table-striped table-hover nowrap" style="width:100%">
        <thead class="bg-warning"></thead>
    </table>
</form>

@section Scripts{
    <script type="text/javascript">
        $(document).ready(function () {
            var table = $('#myTable').DataTable({
                info: false,
                ajax: {
                    url: '@Url.Action("GetData", "Region")',
                    type: 'POST',
                    datatype: "json",
                    error: function (jqXHR, textStatus, errorThrown) {
                        if (jqXHR.status == 403 || jqXHR.status == 401) {
                            window.location = '@Url.Action("Login", "Account")';
                        }
                    }
                },
                "columns": [
                    { data: "@nameof(RegionView.Id)", name: "@nameof(RegionView.Id)", title: "@Html.DisplayNameFor(model => model.Id)" },
                    { data: "@nameof(RegionView.Name)", name: "@nameof(RegionView.Name)", title: "@Html.DisplayNameFor(model => model.Name)" },
                    { data: "@nameof(RegionView.EnName)", name: "@nameof(RegionView.EnName)", title: "@Html.DisplayNameFor(model => model.EnName)" },
                    { data: "@nameof(RegionView.Code)", name: "@nameof(RegionView.Code)", title: "@Html.DisplayNameFor(model => model.Code)" },
                    { data: "@nameof(RegionView.TypeString)", name: "@nameof(RegionView.Type)", title: "@Html.DisplayNameFor(model => model.TypeString)" },
                    {
                        data: null, orderable: false, searchable: false, responsivePriority: -1,
                        render: function (data, type, row) {
                            return "<a href='javascript:editItem(" + data.@nameof(RegionView.Id) + ")' title='@Res.Edit'><i class='fa fa-edit'></i></a>";
                        }
                    },
                    {
                        data: null, orderable: false, searchable: false, responsivePriority: -1,
                        render: function (data, type, row) {
                            return "<a href='javascript:deleteItem(" + data.@nameof(RegionView.Id) + ")' class='link-danger' title='@Res.Delete'><i class='fa fa-times'></i></a>";
                        }
                    }
                ]
            });
        });
    </script>
}